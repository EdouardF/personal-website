import React from 'react';

const ParallaxLayerFooter = () => {
  return (
    <div className="parallax-layer footer">
      {/* Contenu de la couche du pied de page */}
    </div>
  );
};

export default ParallaxLayerFooter;
