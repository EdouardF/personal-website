import React from 'react';

const ParallaxLayerContent = () => {
  return (
    <div className="parallax-layer content">
      {/* Contenu de la couche de contenu */}
    </div>
  );
};

export default ParallaxLayerContent;
