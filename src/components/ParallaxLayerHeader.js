import React from 'react';

const ParallaxLayerHeader = () => {
  return (
    <div className="parallax-layer header">
      {/* Contenu de la couche de l'en-tête */}
    </div>
  );
};

export default ParallaxLayerHeader;
